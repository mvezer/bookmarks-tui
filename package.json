{
  "name": "bookmarks-tui",
  "peerDependencies": {
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "build:connector": "rm -rf connector/dist && bun build ./connector/src/background.ts --outdir ./connector/dist"
  },
  "workspaces": [
    "common",
    "tui-app",
    "connector"
  ],
  "dependencies": {
    "uuid": "^14.0.0"
  }
}
